# ===== BÍ MẬT — KHÔNG BAO GIỜ commit =====
.env
.env.*
!.env.example
*.key
*.pem
secrets/

# ===== FILE RIÊNG TƯ CỦA LEADER (giữ trong máy, KHÔNG đẩy GitHub) =====
# Mọi ghi chú mentor / liên quan tới việc dùng AI để lập kế hoạch nằm ở đây.
# Thầy check GitHub sẽ KHÔNG thấy thư mục này.
_local/

# ===== TÀI LIỆU & BẢN VẼ (đã đưa lên Notion — giữ repo gọn, không commit) =====
# Nội dung đã chuyển sang Notion. Mở local để xem nhưng không đẩy GitHub.
plan/
prototype/
docs/
mss301/

# ===== Build / phụ thuộc =====
node_modules/
dist/
build/
target/
*.class
__pycache__/
*.pyc
.venv/
uv.lock

# ===== Dữ liệu chạy local của RAG / DB =====
data/
assets/books/

# ===== IDE / hệ điều hành =====
.DS_Store
.idea/
.vscode/
*.iml

# ===== Spec do Kiro sinh (giữ trong máy, không cho thầy thấy) =====
.kiro/
